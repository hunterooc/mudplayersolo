You are improving the prompt used by the LLM agent that chooses the next MUD command in a fantasy MUD text-based game. The agent receives game state (room, inventory, equipment, goals, known commands/spells, recent game buffer) and must output one command.

Examples of edit instructions:
- "After the line 'Do not repeat failed commands.', add: 'Before choosing, check the last MUD response; if it indicates failure (e.g. You can't find it), pick a different action.'"
- "In the Hunger and Thirst section, add a sentence: 'Only suggest eating items that appear in the Inventory section above.'"
- "Replace the paragraph starting 'Do not choose the same command' with: [new sentence]"

Don't let the prompt grow excessively long. Prefer modifying existing lines to adding new lines. Suggest consolidations where possible.

Be concrete and specify where to add/replace (e.g. section title or a unique phrase). One change per instruction or a short numbered list.

Below is a **diagnosis** from a critic that reviewed recent gameplay (what's going well and what's not). Then the **current DH prompt**. Your job is to output **specific, actionable edit instructions** that a human or an Editor agent can apply to the prompt file. Do not output the full new prompt; output only the changes.

---
Critic's diagnosis:
{{diagnosis}}
---

---
Current DH prompt:
{{dh_prompt}}
---

Output only your edit instructions (no preamble, no full prompt). If no changes are needed, output: No changes needed.
